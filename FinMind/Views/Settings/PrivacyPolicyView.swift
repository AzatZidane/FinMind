import SwiftUI

struct PrivacyPolicyView: View {
    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 16) {
                Text("Политика конфиденциальности FinMind")
                    .font(.title2)
                    .bold()

                Text("Дата обновления: 6 сентября 2025 г.")
                    .font(.subheadline)
                    .foregroundColor(.secondary)

                Group {
                    Text("1. Кто мы").bold()
                    Text("FinMind — персональное офлайн-приложение для учёта финансов. Контакты: ismagilowazaat@yandex.ru")
                }

                Group {
                    Text("2. Какие данные обрабатываются").bold()
                    Text("• Введённые пользователем данные: доходы, расходы, категории, цели и долги; профиль (аватар, никнейм, тема/валюта); по желанию — сбережения (фиат/криптовалюта/металлы).")
                    Text("• Резервные копии и экспорт/импорт (JSON) — только по инициативе пользователя.")
                    Text("• Технические данные соединения (IP-адрес, время запроса) могут кратковременно обрабатываться при использовании онлайн-функций и не используются для идентификации.")
                    Text("• ИИ-советник: текст запроса обрабатывается через сервер-прокси FinMind с использованием технологий OpenAI.")
                }

                Group {
                    Text("3. Цели обработки").bold()
                    Text("• функционирование приложения;")
                    Text("• опциональная синхронизация через iCloud (если включена);")
                    Text("• поддержка и отладка (если пользователь вручную отправляет отчёт об ошибке).")
                }

                Group {
                    Text("4. Хранение данных").bold()
                    Text("Данные бюджета и настроек хранятся локально на устройстве. При включении iCloud синхронизация регулируется правилами Apple. Запросы к ИИ обрабатываются через прокси FinMind и поставщика ИИ.")
                }

                Group {
                    Text("5. Передача третьим лицам").bold()
                    Text("OpenAI (через прокси FinMind) — для обработки ИИ-запросов; Apple (iCloud) — если включена синхронизация. Данные не продаются и не используются для рекламы.")
                }

                Group {
                    Text("6. Права пользователя").bold()
                    Text("• экспорт/импорт данных (JSON);")
                    Text("• удаление всех данных: «Настройки → О приложении → Удалить все данные…»;")
                    Text("• отключение iCloud-синхронизации;")
                    Text("• обращение к разработчику: ismagilowazaat@yandex.ru.")
                }

                Group {
                    Text("7. Безопасность").bold()
                    Text("Передача данных по сети защищена (HTTPS). Мы принимаем меры для защиты данных, однако абсолютная безопасность не гарантируется.")
                }

                Group {
                    Text("8. Дети").bold()
                    Text("Приложение не предназначено для лиц младше 13 лет.")
                }

                Group {
                    Text("9. Изменения политики").bold()
                    Text("Актуальная версия и дата обновления публикуются на этой странице и в приложении.")
                }

                Group {
                    Text("10. Контакты").bold()
                    Text("Email: ismagilowazaat@yandex.ru")
                }
            }
            .padding()
        }
        .navigationTitle("Политика")
        .navigationBarTitleDisplayMode(.inline)
    }
}

#Preview { PrivacyPolicyView() }
